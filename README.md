# tl1-tp1-2024-joaquinlechesi
tl1-tp1-2024-joaquinlechesi created by GitHub Classroom
TP1
### ¿Por qué es conveniente incluirlo?
Para evitar la lectura no necesaria de archivos _generados_ por los programas que utilizamos a diario.
### ¿Cuándo se debe hacer?
Cuando nos vamos a centrar en algunos archivos especificos o solo nos interesa visualizar de manera mas **ordenada** los archivos.
### ¿Cómo configuraría el archivo .gitignore?
Se abre con un editor de texto y se agregan "reglas" que especifican que archivos se van a tener en cuenta y cuales vas a a ser ignorados.
Ejemplo:
ignorado.txt

3.g)
El resultado es el mismo ya que a una **variable** le corresponde una direccion de memoria y la funcion que cumple las **variables puntero** es almacenar la direccion de memoria de una variable del mismo tipo, para el caso del apartado 2) y 3) el valor es el mismo con el cuantificador **%p**, en el caso del puntero es mostrar el dato almacenado y con el variable es usar el operador **&**. En el punto 4) podemos obserbar que la variable y el puntero que al cual se le asigno esta misma variable poseen una direccion de memoria contigua debido a que son del mismo tipo, se declaró primero la variable puntero y la variable, ambos del tipo __int__ por lo cual el salto de una direccion a otra es de __4 bits__,.